export const environment = {
  production: false,
  /** Trailing slash required — paths are appended after `/api/`. */
  apiBaseUrl: 'https://alhendalcompany-001-site11.jtempurl.com/api/',
  surveySlug: 'bubble-hope',
};
